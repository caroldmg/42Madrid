//añadir header 42

char    *ft_itoa(int n)
{
    // he oido por ahi que hay que hacer un contador de la cantidad de cifras del n
    // para saber cuánto espacio reservamos con malloc
    // algo relacionado con el max y el min int?
    // será similar al putnbr pero en vez de escribirlo lo guardamos en un char *
}